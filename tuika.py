class Sliding(pg.sprite.Sprite):
    """
    スライディングに関するクラス
    """
    def __init__(self):
        img0 = pg.transform.rotozoom(pg.image.load(f"ex05/Sliding.png"), 0, 2.0)
        img = pg.transform.flip(img0, True, False)
        self_imagi = self_imagis[]


sliding = pg.sprite.Group()

if event.type == pg.kEYDOwN:
     if event.key == pg.K_DOWN:
         sliding.add()